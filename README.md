# Priority_Queue-max-minheap-
A priority queue is a type of queue where each element is associated with a priority value, and elements are served based on their priority rather than their insertion order.
